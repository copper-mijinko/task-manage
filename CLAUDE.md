@AGENTS.md

# Claude Code integration

Use the project-local `.mcp.json` entry named `task-manage-ui`. It connects Playwright MCP to the Agent UI Electron CDP endpoint; it is not a normal browser target. If Claude Code has not loaded that server, restart or re-approve the project MCP configuration and report GUI verification as unavailable until it is active.
