# task-manage 仕様書

← [outline.md](outline.md)

この文書は、今時点の実装を断面として説明するための仕様書である。
データ保存・データ構造の詳細は [data.md](data.md) を参照。

## 1. 概要

`task-manage` は、Electron と Svelte で構成されたデスクトップ向けタスク管理アプリケーションである。

利用者は、プロジェクトごとにタスクをツリー構造で管理できる。
各タスクには、名前、状態、期限日、メモを持たせられる。
また、テーマ切替、タスク検索、ページ内検索、別ウィンドウでの詳細表示にも対応している。

本アプリはローカル保存を前提としており、データは JSON ファイルとして保存される。

## 2. 技術構成

- UI: Svelte 5
- デスクトップ実行環境: Electron
- ビルド: Vite
- 言語: TypeScript と JavaScript の混在構成
- ローカル保存: `@commonify/lowdb`（`db.json` / `meta.json`）
- ファイル監視: `chokidar`（ワークスペースの外部書込検知）
- テスト:
  - `Vitest`
  - `@testing-library/svelte`
  - `Playwright`

## 3. ディレクトリ構成

現在の主なディレクトリ構成は次のとおりである。

```text
task-manage/
├─ docs/
│  ├─ outline.md
│  ├─ specification.md
│  ├─ data.md
│  ├─ architecture.md
│  ├─ testing.md
│  └─ how-to-contribute.md
├─ dist/
│  └─ （electron-builder 成果物。git 管理外）
├─ renderer/
│  └─ （Vite ビルド成果物。git 管理外）
├─ electron/
│  ├─ index.js
│  ├─ preload.js
│  ├─ workspace.js
│  ├─ workspace-write-queue.js
│  └─ workspace-reconciler.js
├─ public/
│  ├─ global.css
│  └─ static/
├─ src/
│  ├─ lib/
│  ├─ features/
│  ├─ pages/
│  ├─ stores/
│  ├─ types/
│  ├─ App.svelte
│  ├─ main.ts
│  ├─ stores.ts
│  └─ global.d.ts
├─ tests/
│  ├─ unit/
│  ├─ component/
│  ├─ e2e/
│  └─ setup/
├─ index.html
├─ package.json
├─ vite.config.js
├─ tsconfig.json
├─ vitest.config.mjs
└─ playwright.config.js
```

各ディレクトリの役割は次のとおりである。

- `docs/`
  - プロジェクト文書を置く
- `dist/`
  - electron-builder のパッケージング成果物を置く
- `renderer/`
  - Vite のビルド成果物を置く。Electron 本番起動時のロード先
- `electron/`
  - Electron の main process と preload script を置く
- `public/`
  - Vite の publicDir。ビルド時に `dist/` へそのままコピーされる静的ファイルを置く
- `src/`
  - アプリ本体の UI と状態管理ロジックを置く
- `tests/`
  - 自動テストを置く

## 4. 実行アーキテクチャ

本アプリは、Electron の main process と renderer process で役割分担している。

### 4.1 main process

`electron/index.js` が担当する。
主な責務は次のとおりである。

- アプリウィンドウの作成
- タスク詳細用サブウィンドウの作成
- JSON ファイルの読み書き
- IPC の受け口
- `findInPage` を使ったページ内検索
- 複数ウィンドウ間のデータ同期通知
- ワークスペース保存キュー（`electron/workspace-write-queue.js`）の管理
- ワークスペース変更監視と外部書込の取り込み（`electron/workspace-reconciler.js`）

### 4.2 preload

`electron/preload.js` が担当する。
renderer から直接 Node API を触らずに済むよう、`window.electronAPI` を公開する。

### 4.3 renderer

`src/` 配下が担当する。
Svelte コンポーネントと store を使い、画面表示と UI 操作を処理する。

主な入口は次のとおりである。

- `src/main.ts`
  - Svelte アプリの起動
- `src/App.svelte`
  - 画面全体のルート
- `src/stores.ts`
  - アプリ全体の状態管理

## 5. 画面構成

### 5.1 メイン画面

メイン画面は次の領域で構成される。

- ヘッダー
  - サイドバートグル（ハンバーガー ⇄ `＜` で開閉状態を表す）
  - タイトル
  - 画面内ハイライト検索ボックス（後述 5.3）
  - Inbox ボタン（📥、件数バッジ付き。クリックで Inbox を開く / `Ctrl+Shift+I` で QuickCapture モーダル）
  - 保存状態インジケータ
  - テーマ切替
  - 設定ボタン（⚙ アイコン。クリックで設定モーダルを開く）
- 左サイドバー（常駐型。起動時は折りたたみ状態）
  - Workspace セクション（Workspace 管理ボタンのみ）
  - Projects セクション
    - Workspace プロジェクト一覧（削除ボタン付き。Inbox アイテムの D&D 受け入れ対象）
    - InApp プロジェクト一覧（`db.json` ベース。追加・削除ボタン付き）
  - Info 一覧
  - Tags フィルタ（他セクションと同一の `Section` + `Contents` レイアウト、検索入力 + 件数バッジ）
- 中央から右側
  - タスクツリー + ガントチャート（横分割の `SplitPanes`）
  - タスク詳細 + メモ（単一 Card 内で上下分割）

左サイドバーは Drawer ではなく永続的に表示するペインで、ヘッダーのトグルで折りたたみ状態を切り替える。
状態はセッション間で保持せず、起動時は常に折りたたまれている。
トグルアイコンは折りたたみ時はハンバーガー、展開時は `＜` 形状に切り替わる。

### 5.2 タスクツリー画面

タスクツリー画面は左右 2 ペイン構成である。

- 左ペイン（`Card` でラップ。タイトルバー付き）
  - Card タイトルはプロジェクト名を表示し、横に `Workspace` / `InApp` の Storage ラベルを表示する
  - ツールバー
    - タスク追加 / 子タスク追加 / 削除
    - 上に移動 / 下に移動 / アウトデント / インデント
    - すべて展開 / すべて折りたたみ
    - 元に戻す / やり直し
    - 全メモを Markdown / Quill に一括変換
    - 「タスクをフィルタ」検索ボックス（後述 5.3）
    - ガント表示切替 / 詳細ペイン表示切替
  - ツリーテーブル
    - 列ヘッダー（ソート、フィルタ、列設定ポップオーバー）
    - スティッキーブレッドクラム（ヘッダ直下、行高は本体行と完全一致）
- 右ペイン（`Card` でラップ。単一 Card 内で上下分割）
  - Card タイトルは選択中タスク名を表示する
  - 上段: 選択中タスクの詳細（Name / Status / Start Date / Due Date / Memo 数）
  - 下段: メモ編集（タブ + タグ + Quill or Markdown 本文）
  - 初期状態では上段を内容に必要な高さへ収め、下段のメモ領域を最大化する
  - 上段と下段の境界は Card 本体に直接実装したリサイザーで調整できる。`SplitPanes` と同様にドラッグ中は自由移動し、リリース時に Card ヘッダ下線または Card 下端へ吸着する
  - 分割は `TaskDetail.svelte` が直接実装し、`SplitPanes` コンポーネントのネストは使用しない

タスクツリーの行は CSS のみのフェードイン（`@keyframes RowEnter`）でアニメーション表示する。
展開・折りたたみのキャレットも CSS トランジションで回転する。
Svelte の transition は使用しない。

### 5.3 検索系の役割分担

本アプリには次の 2 種類の検索系がある。役割は明確に分離されている。

- ヘッダーの「画面内をハイライト検索」ボックス（`Ctrl+F` でフォーカス）
  - 表示中ウィンドウ内のテキストノードをスキャンしてマッチをハイライトする
  - 表示行を絞り込まない
  - 一致件数を `N / M` 形式で表示
  - `↑` / `↓` ボタン、`Enter` / `Shift+Enter`、`F3` / `Ctrl+G` で前後の一致へ移動、現在の一致は別色でフォーカス表示
  - 折りたたみ済みノード、`display: none`、ゼロサイズ要素、検索ボックス自身の入力欄はスキャン対象から除外する
  - 実装は CSS Custom Highlight API を使用し、DOM を書き換えずに Quill / Markdown / ツリー行など全テキストノードを同時にハイライトする
- ツールバーの「filter tasks」ボックス
  - タスクツリーの表示行を絞り込む
  - メモ本文も検索対象に含めるか切替できる
  - 検索ボタンとメモ検索切替ボタンに個別ツールチップが付く

### 5.4 タスク詳細ウィンドウ

タスクは別ウィンドウでも詳細表示できる。
このウィンドウでは、右ペイン相当の編集内容を独立して表示する。
ウィンドウタイトルおよび見出しでタスク名を示すため、内部の Card タイトルは非表示にする。

## 6. 主な機能

### 6.0 Inbox（ワークスペース横断のクイックキャプチャ）

Inbox は「とりあえず思いついたタスクを入れて、後でプロジェクトに振り分ける」ための専用バケットである。プロジェクトには紐づかず、ワークスペース全体で 1 つ持つ。

- **スコープ**：アクティブなワークスペースに 1 つ。ワークスペースを切り替えると Inbox も切り替わる。Workspace 未設定時は Inbox 機能は無効化される
- **データ位置**：`<workspace>/_inbox/` 配下に専用ディレクトリとして実体を持つ。`_` で始まるためプロジェクト一覧には現れない
- **構造**：常にフラット（ルート1 + 直下に Inbox アイテムの並びのみ）。ツリー化・インデントは存在しない
- **アイテムの持つフィールド**：通常タスクと同じ（Name / Status / Start Date / Due Date / Memo）

#### 入り口

「Inbox を開く」入り口と「Inbox に追加する」入り口は別軸で、それぞれ UI 上は 1 つに集約されている。サイドバーには Inbox 行を置かない（D&D の落とし先としてのみワークスペースプロジェクト行が機能する）。

- **Inbox を開く**：ヘッダの 📥 ボタン
  - クリックで Inbox ビューを開く（`selected_type = "Inbox"`）。ボタンは Active 表示になる
  - 件数バッジ：未処理 Inbox アイテムが 1 件以上あるとボタン右上に件数を表示する
- **Inbox に追加する**：Quick Capture モーダル
  - 起動経路（同じモーダルを呼び出す 2 ルート）：
    - InboxPanel ツールバー先頭の「追加」ボタン
    - グローバルショートカット **`Ctrl+Shift+I`**：テキスト編集中（input/textarea/contenteditable/Quill/CodeMirror）でも素通しせず Quick Capture が起動する
  - **Inbox を開いた瞬間も自動でモーダルを起動**：InboxPanel のマウントで `showQuickCapture = true` を立てる。ユーザーが意図して Inbox に来たタイミング＝何かを入れたいタイミング、という前提に従う。Esc または モーダル外クリックで閉じて通常の Inbox ビューに戻る

#### Inbox ビュー

`SplitPanes` で左右 2 ペイン構成：

- **左ペイン**：Inbox アイテムのフラットリスト
  - ツールバー：削除、プロジェクトへ送る ▼、全選択／解除、絞り込み、件数表示
  - 各行：ドラッグハンドル、チェックボックス、Name（インライン編集）、Status（StatusSelect）、Due（DateInput）、メモ件数バッジ
  - 行クリックで選択＋アクティブ化（複数選択は Ctrl/Shift クリック）。アクティブ行は左に Primary 色のリボン
- **右ペイン**：アクティブアイテムの詳細編集
  - Name / Status / Start Date / Due Date / Memo 数の表示
  - `MemoTab` を組み込み、タブ切替・追加・削除・並べ替え・Markdown/Quill 切替・画像ペーストが行える
  - 未選択時は使い方ヒントを表示

#### Quick Capture モーダル

- 任意の画面から `Ctrl+Shift+I` で起動する常駐モーダル
- Enter で追加して入力欄をリセット（連続追加できる）、Shift+Enter で追加して閉じる、Esc で閉じる
- 直近追加 5 件を画面下部に表示してフィードバックを返す

#### 整理（プロジェクトへ振り分ける）

送信経路は **モーダルピッカー** に統一する。サイドバーはアプリ内で Drawer として運用されており（起動時折りたたみ、必要時のみ展開）、常時可視を前提とする D&D の落とし先には向かないため、サイドバーへのドロップは UI 上提供しない。

- **モーダルピッカー**：Inbox ツールバーの「プロジェクトへ送る…」をクリックするとモーダルが開く
  - 送信先プロジェクト選択（既定はアクティブなプロジェクト）
  - そのプロジェクトのツリーが展開可能な状態で表示される
  - 任意のノードをクリックして親に指定する（ルート行＝プロジェクト直下も選択可）
  - ノード名で絞り込み検索できる
  - フッターに送信先パス（"ProjectA / 設計"）と件数を表示
  - 「送る」で確定。送信されたアイテムは指定した親の **子の末尾** に追加される
- **Inbox 内 D&D**：Inbox アイテム行同士のドラッグはリスト内の **並べ替え** にだけ使う。上半分にドロップ＝前に挿入、下半分＝後に挿入。複数選択ドラッグも連続ブロックとして移動する
- 送信されたアイテムはディレクトリごと移動するため、メモ・assets/ の相対パスは壊れない
- 移動方向は Inbox → Project の一方向のみ。プロジェクト内タスクを Inbox に戻すパスは持たない
- 送信先は Workspace プロジェクトのみ（InApp プロジェクト `db.json` への送信は対象外）

### 6.1 プロジェクト管理

- 標準プロジェクト（`db.json`）
  - プロジェクトの追加
  - プロジェクトの削除
  - プロジェクト順のドラッグ＆ドロップ変更
  - 並び順の保存
- ワークスペースプロジェクト（ディレクトリ + Markdown）
  - ワークスペースの追加・切替・削除
  - ワークスペースプロジェクトの作成
  - ワークスペースプロジェクトの削除（確認ダイアログ + IPC `ws:delete-project`）
  - ワークスペース管理ボタンはアイコン + 「管理」テキストの明示的なボタン
  - 外部書込（OneDrive 同期、手動編集など）を検知した場合は通知バナーを表示する
  - ローカルにペンディング書込がある状態で外部書込を検知した場合はコンフリクトバナーを表示し、「維持」または「再読込」を選ばせる
  - `db.json` プロジェクトをワークスペースへエクスポートできる（一方向のみ。逆方向は不可）
    - エクスポート時にフォーマット保持または全メモ Markdown 変換を選択できる
    - エクスポート先のプロジェクトとメモには新規 UUID を発行する（ソース ID は引き継がない）

### 6.2 タスク管理

- タスクの追加 / 子タスクの追加 / タスク削除
- ルートが選択されている状態で「下に追加（兄弟挿入）」を押下した場合は、操作に意味がないためアラート表示する（既存の Dialog アラート流路に乗せる）
- タスク名の編集（三点リーダメニューの「rename」から）
- コピー / 子としてペースト
- ステータス変更（自前実装のドロップダウン。状態色はチップに反映するが、選択肢リスト本体には反映しない）
- 開始日 / 期限日変更
- メモ編集
- ワークスペースタスクでは、タスク詳細欄から添付ファイルを追加できる
  - 添付ボタンは OS のファイル選択を開き、複数ファイルをまとめて追加できる
  - 添付欄へのファイル drag & drop でも同じ保存経路で追加できる
  - 添付は対象タスクの `attachments/` ディレクトリに保存し、タスクツリーの `attachments` 列には件数を表示する
  - 添付ファイルはクリックで既定アプリから開き、右クリックメニューから「開く」と「プログラムから開く」を選択できる
- タスク詳細を別ウィンドウで開く（`TaskDetail` Card ヘッダ右側のアイコンボタン。サブウィンドウ内（`hideDetailTitle` 有効時）はヘッダごと表示しないため再帰起動の余地はない）

### 6.3 ツリー操作

- ノードの展開 / 折りたたみ（CSS のみのフェードインアニメーション）
- 行ドラッグ＆ドロップによる移動。落とす位置の高さで 3 通りに分岐する
  - 上 1/3 にドロップ → 兄として挿入（前に挿入）
  - 中央 1/3 にドロップ → 子として追加（append）
  - 下 1/3 にドロップ → 弟として挿入（後に挿入）
- 三点リーダメニューによる操作
  - rename / add task below / add child / copy / paste as child / move up / move down / move right / move left / delete task
  - 「別ウィンドウで開く」アクションは行メニューには含めず、`TaskDetail` Card のヘッダ右側に置いた IconButton から行う（選択中タスクを別ウィンドウへ）
  - メニューは再度三点リーダボタンを押下すると閉じる
  - 画面上の任意の場所のクリックで閉じる（disabled なボタンや Quill / Markdown の未作成領域も含む）。実装は capture phase の `pointerdown` + `mousedown` を listen する `globalDismiss` action
- 列ヘッダー
  - ソート（昇順 / 降順 / 解除のサイクル）
  - 列ごとのフィルタポップオーバー（Name / Status / Start Date / Due Date / メモ数）
  - 列設定ポップオーバー（表示 / 非表示 / 並べ替え）
  - フィルタポップオーバーと列設定ポップオーバーも `globalDismiss` で外部クリックや他ボタン押下で閉じる
- 列幅
  - 最終列はペイン幅に合わせて伸縮する。余白は最終列が埋める
  - 他列はユーザがドラッグした絶対幅を維持する。折りたたみ・展開・列設定操作で勝手にリサイズされない
  - 列の最小幅は 8rem
- スティッキー表示
  - スクロール時、現在の親階層をブレッドクラム形式でツリーヘッダ直下に固定表示する
  - 行高さは本体行と完全一致（2.5rem）
- タスク名のハイライト表示は CSS Custom Highlight API による画面内検索でカバーされる

### 6.4 検索とフィルタ

「5.3 検索系の役割分担」のとおり 2 系統。

- 行絞り込み（filter tasks）
  - タスク名による部分一致
  - ステータスの複数選択
  - メモ本文を検索対象に含めるか切替できる
- 画面内ハイライト検索
  - 表示中ウィンドウ内の全テキストノードをハイライト
  - 一致件数の表示と前後移動
  - 折りたたみ済みノードや非表示要素はスキャン対象から除外

### 6.5 ペイン操作

- 水平 / 垂直の `SplitPanes` でリサイズ可能
- リサイザーをペイン端まで持っていくと、マウスアップ時にペインが折りたたまれる
  - ドラッグ中はカーソルに完全追従し、しきい値で止まらない
  - マウスアップ時、ペイン幅が「最小幅 × 0.6」を下回っていればペインを 0px に折りたたむ
  - 折りたたまれたペインは `min-width: 0` でインラインスタイル指定するため、CSS 側の `min-width` 指定があっても確実に非表示になる
- 折りたたみ済みペインの隣のリサイザーは、ヒット領域が広くなり Primary 色に変わる。隠れていることが視覚的にわかり、再展開時のターゲットも大きくなる
- 折りたたんだ後は同じリサイザーを反対方向にドラッグすれば再展開できる

### 6.6 ガントチャート

- 列スケール: 日 / 週 / 月
  - 「日 / 週 / 月」ボタンはガントヘッダ上にセグメント風で表示し、アクティブ状態を白背景 + 強調色で識別できる
  - 週表示時のラベルは `mm/dd ～ mm/dd` 形式
- 今日（today）の表示
  - ヘッダおよびボディとも 1 日分の Accent 色ストライプ（最小 8px）として描画
  - ヘッダには 2px の縦線、ボディには 1px の縦線も Accent 色で重ねる
  - 週・月表示でも視認可能な太さを保つ
- 初回マウントおよびスケール切替時、今日が画面中央付近に来るよう自動スクロールする
- バー
  - 開始日 + 期限日が両方ある → 期間バー（オーバードゥ部分は赤色）
  - 期限日のみ → 1 日分の丸マーカー
  - 開始日のみ → 1 日分の丸マーカー（インセットアウトラインで開始マーカーと識別）
  - inherited（親の日付を継承）→ 半透明バー（編集不可）
- 操作
  - バー全体をドラッグ → 期間を移動
  - 開始ハンドル / 終了ハンドルをドラッグ → 開始 / 終了を変更
  - 空セルを drag-create → 範囲を新規作成
- スクロール
  - ユーザの編集で timeline が広がっても、現在のスクロール位置を保持する（不当な視点移動を抑止）

### 6.7 メモ

- メモごとにフォーマット（Markdown / Quill）を持つ。フォーマットの保存形式・デフォルト・一括変換の挙動は [data.md](data.md) § 6 を参照
- 各メモのフォーマット切替と、Markdown の Read / Edit モード切替はいずれも `SegmentedControl` を使用する
- タグエリア
  - 入力フィールドのフォーカス時は、外側のリングのみ Primary 色に。内側のボーダーは色を変えない
  - サイドバーの Tags フィルタへ即時反映する
- Markdown
  - 編集 / プレビュー間の分割リサイザーは他の SplitPanes と同じ薄いハンドルデザイン
  - Quill の上端の二重ボーダーは解消（`.ql-container` の上ボーダーを非表示）

### 6.8 Card UI

- 共通の `Card` プリミティブは `title` と `padded` プロパティを受ける
- タイトルが指定された場合、ヘッダ行全体に半透明の Primary 色背景 + 下罫線を引く
- ホバー時に elevation を一段上げ、Card 感を強調する

### 6.9 ツールチップ

- 残存防止のためグローバルクリーンアップを行う
  - スクロール / mousedown / keydown / 可視性変更 / blur のたびに掃除
  - 一定間隔（1 秒）でも生存中のツールチップのみ掃除する。アクティブなツールチップがない時はタイマーを動かさない
  - 表示元のアンカー要素が DOM から消えた / hover が外れた場合は確実に削除
- 表示中にプロパティが変わると、テキスト・色を即座に更新する（サイドバー開閉時のラベルなど）

### 6.10 テーマ

- ダークテーマ
- ライトテーマ

テーマは保存され、再起動後も維持される。
ガントチャートの今日表示には Accent カラー（`#FF2E63`）を用いる。Primary 系と被らないため両テーマで視認可能。

### 6.11 入力ショートカット

テキスト入力中に次のショートカットで現在の日時を挿入できる。

- `Ctrl+;` — 現在の日付
- `Ctrl+:` — 現在の時刻

対象は以下すべてで、いずれのケースでも各エディタの Undo 履歴に統合される。

- ネイティブの `<input>` / `<textarea>`
- CodeMirror（Markdown メモ）
- Quill（リッチテキストメモ）
- その他 `contenteditable` 要素

挿入される文字列は設定モーダルの「日時フォーマット」で次の 3 種から選択する（既定: スラッシュ区切り）。

- `2026/05/26` / `14:30`
- `2026-05-26` / `14:30`
- `2026年5月26日` / `14時30分`

ただし `<input type="date">` / `<input type="time">` / `<input type="datetime-local">` などのネイティブピッカーでは、HTML 仕様上 ISO 形式の値が必須のため、フォーマット設定にかかわらず `YYYY-MM-DD` / `HH:MM` 形式の値が直接セットされる。

### 6.12 設定モーダル

ヘッダの ⚙ ボタンから開くアプリ全体の設定モーダル。画面の 80% を占める大きさで、左にカテゴリ一覧、右に選択中カテゴリの設定 UI を表示する 2 ペイン構成。

現在のカテゴリ:

- **日時フォーマット** — 入力ショートカット（§6.11）で挿入される書式を `SegmentedControl` で切替。現在の書式によるプレビューも表示する

## 7. 状態管理

アプリ全体の状態は `src/stores/` 配下で管理している。`src/stores.ts` は互換のための再エクスポートのみ。
主な状態は次のとおりである。

- プロジェクト一覧
- 現在のプロジェクト
- 現在の選択タスク
- 現在のツリーデータ
- フィルタ条件
- ページ内ハイライト検索クエリと一致件数 / 現在位置
- テーマ
- ノード開閉状態
- 左サイドバーの折りたたみ状態（永続化はしない。起動時は常に折りたたみ）
- コピー中タスク（paste as child 用）
- 日時ショートカットのフォーマット設定（`@stores/preferences` の `date_time_format`、永続化あり）

renderer で状態が変わると、必要に応じて `window.electronAPI` 経由で保存や同期処理を行う。

## 8. 現在の実装上の挙動

### 8.1 起動

- 起動時に保存済みプロジェクトがあれば、先頭プロジェクトを開く
- プロジェクトが無い場合は `No data.` を表示する

### 8.2 タスク操作

- ルートノードは削除できない
- ワークスペースプロジェクトを開いた時も、プロジェクト root はツリーのルート行として表示される
- タスク未選択時にタスク追加を行った場合は、ルートノード配下へ最初のタスクを追加する
- ルートノードを明示的に選択している状態で「下に追加（兄弟挿入）」を押下した場合は、ダイアログで `Cannot insert a sibling at the project root.` をアラート表示する。子として追加する場合は「子タスク追加」ボタンを使う
- タスク追加時は、追加された行が選択される
- 子タスク追加時、親が折りたたみ状態なら自動展開する

### 8.3 複数ウィンドウ

- 別ウィンドウで開いたタスク詳細は編集できる
- 別ウィンドウでの編集内容は、同一プロジェクトを開いている他ウィンドウへ同期される
- 対象タスクが削除されると、サブウィンドウでは `Task not found.` を表示する
- 対象プロジェクトが削除されると、サブウィンドウでは `Project not found.` を表示する

### 8.4 メモとリンク

- メモ内の通常リンクや外部 URL wiki link をクリックすると外部ブラウザで開く
- メモ内の同一タスク内 wiki link をクリックすると、同名のメモタブへ移動する
- メモ編集中に画像をペーストすると、保存モードに応じて workspace assets または data URL として Markdown 画像が挿入される
- リンクを開けなかった場合はメモ上部にエラーバナーを表示し、×ボタンで閉じられる
- リンク処理中は重複クリックを無視し、処理完了後に次のクリックを受け付ける

### 8.5 サイドバー

- 左サイドバーは Drawer 形式ではなく永続表示で、起動時は常に折りたたまれる
- ヘッダのハンバーガー / `＜` トグルアイコンは折りたたみ状態を視覚的に表す

### 8.6 ガントチャート

- 起動時とガントスケール切替時、ガントの今日列が画面中央付近に来るよう自動スクロールする
- ガントで日付を編集してもタイムライン窓は monotonic に拡大のみ。ユーザのスクロール位置は保持する

### 8.7 ペイン

- タスクツリーのリサイザーをペイン端までドラッグしてマウスアップすると、ペインが折りたたまれる。再ドラッグで展開できる
- 折りたたみ済みペインの隣のリサイザーはヒット領域が拡大され、Primary 色で強調表示される

### 8.8 画面内検索

- メモ内 Markdown プレビューを画面内検索すると、CSS Custom Highlight API によって本文中の一致箇所が直接ハイライトされる

### 8.9 ワークスペース保存

- ワークスペースプロジェクトの保存は UI 操作と非同期で進行し、main プロセスのキューが直列に書き出す。ヘッダの保存状態インジケータは `保存待ち` / `保存中...` / `再試行中` / `保存済み` / `保存失敗` / `競合` の 6 状態を表示する
- 通常編集では前回保存時のメモリ状態との差分から変更タスクと削除 task id だけを保存キューへ送り、初回保存や前回データがない場合のみ全体スナップショット保存にフォールバックする
- ワークスペース配下のファイルが外部から書き換えられた場合、未保存のローカル変更がなければ自動で取り込みバナーを表示する
- ローカルに未保存変更がある状態で外部書込を検知した場合は、ヘッダの保存状態を `競合` にしてバナーを表示し、`維持`（ローカル優先で次回保存時に上書き）または `再読込`（ローカル変更を破棄して取り込み）を選ばせる
- ファイル名に `conflicted copy` を含むファイルが現れた場合は通知バナーで知らせるが、自動マージは行わない
- アプリ終了時、ワークスペース保存キューにペンディングが残っていれば、書き出し完了を待ってから終了する

## 9. 開発時によく使うコマンド

- `npm run dev`
  - Vite dev server を起動し、Electron を自動起動する。HMR によりファイル変更が即時反映される
- `npm run build`
  - Vite で配布用ビルドを `dist/` に生成する
- `npm run start`
  - ビルド済みの `dist/` を読み込んで Electron を起動する
- `npm run check`
  - 型と Svelte の基本チェックを行う
- `npm run test:unit`
  - unit テストを実行する
- `npm run test:component`
  - component テストを実行する
- `npm run test:e2e`
  - E2E テストを実行する
